<%= form_with(model: wish_item, local: true) do |form| %>
  <% if wish_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wish_item.errors.count, "error") %> prohibited this wish_item from being saved:</h2>

      <ul>
        <% wish_item.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.text_field :item_id %>
  </div>

  <div class="field">
    <%= form.label :wishlist_id %>
    <%= form.text_field :wishlist_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
